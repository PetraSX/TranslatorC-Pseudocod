#include <bits/stdc++.h>

using namespace std;

ifstream in("date.in");
ofstream out("date.out");

int main()
{
    int n, a, b;
    cin >> n >> m >> p >> k >>j;
    a = n;
    b = n%2;
    for(i = 1; i <= n; i++)
    {
    }
    if(b == 0)
    {
        if(b%2 !=0)
        {
            a = 2;
            b = 3;
        }
        cout<<a;
        a = 0;
    }
    if(b == 1)
    {
        cout << a;
        a = 1;
    }

    cout << ' ' << a;
    return 0;
}